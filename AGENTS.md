# Project rules

- Keep all visible copy in natural Russian and write as one independent specialist.
- Use Astro components, TypeScript, Tailwind CSS, and minimal vanilla browser JavaScript.
- Keep `BrandMark` text-based and keep `/brand-preview` unlinked and `noindex`.
- Mark every fictional portfolio example as `Концепт`; never invent clients, reviews, or results.
- Update contact placeholders only in `src/config/site.ts`.
- Run `npm run check` and `npm run build` after implementation changes.
