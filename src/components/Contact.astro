<section class="section contact" id="contact" aria-labelledby="contact-title">
  <div class="container">
    <div class="contact-shell" data-reveal="scale">
      <div class="contact-glow" aria-hidden="true"></div>
      <div class="contact-main">
        <p class="section-label" data-reveal="fade">Контакты</p>
        <h2 id="contact-title">
          <span class="contact-title-line" data-reveal="up"><span>Есть проект,</span></span>
          <span class="contact-title-line" data-reveal="up" data-delay="80"><span>который стоит запомнить?</span></span>
        </h2>
        <p class="lead" data-reveal="up" data-delay="140">Расскажите о задаче, продукте или идее. Я предложу направление и формат работы.</p>
        <div class="contact-actions" data-reveal="up" data-delay="220">
          <a class="button button-primary" href="mailto:hello@tsblv.com" data-magnetic>Написать мне</a>
          <a class="contact-email" href="mailto:hello@tsblv.com">hello@tsblv.com</a>
        </div>
      </div>
      <p class="contact-location" data-reveal="up" data-delay="300">
        <span aria-hidden="true">Æ</span>
        Северная Осетия — работа по всему миру
      </p>
    </div>
  </div>
</section>

<style>
  .contact {
    border-top: 0;
    padding-bottom: clamp(4rem, 9vw, 7rem);
  }

  .contact-shell {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    padding: clamp(2.5rem, 8vw, 6.5rem);
    border: 1px solid var(--color-line);
    border-radius: clamp(1.5rem, 3vw, 2.5rem);
    background:
      linear-gradient(155deg, rgba(48, 51, 48, 0.42), transparent 38%),
      var(--color-surface-raised);
  }

  .contact-glow {
    position: absolute;
    z-index: 0;
    top: -45%;
    right: -12%;
    width: min(44rem, 80vw);
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(circle, var(--color-glow-warm), transparent 68%);
    pointer-events: none;
  }

  .contact-main {
    position: relative;
    z-index: 1;
  }

  h2 {
    max-width: 14ch;
    margin-bottom: clamp(1.5rem, 3vw, 2rem);
  }

  h2 span {
    display: block;
  }

  .contact-title-line {
    display: block;
  }

  .lead {
    margin-bottom: clamp(2rem, 4vw, 3rem);
  }

  .contact-actions {
    display: flex;
    align-items: flex-start;
    flex-direction: column;
    gap: 1.25rem;
  }

  .contact-email {
    color: var(--color-text-muted);
    font-size: var(--font-size-ui);
    text-decoration-color: transparent;
    transition: color var(--duration-fast), text-decoration-color var(--duration-fast);
  }

  .contact-email:hover,
  .contact-email:focus-visible {
    color: var(--color-text);
    text-decoration-color: var(--color-accent-soft);
  }

  .contact-location {
    position: relative;
    z-index: 1;
    margin-top: clamp(4rem, 10vw, 7rem);
    color: var(--color-text-muted);
    font-size: var(--font-size-meta);
    letter-spacing: 0.02em;
  }

  .contact-location span {
    margin-right: 0.5rem;
    color: var(--color-accent);
    font-family: var(--font-display);
    font-size: 1.1rem;
  }

  @media (min-width: 36rem) {
    .contact-actions {
      align-items: center;
      flex-direction: row;
      gap: 1.75rem;
    }
  }
</style>
