---
import BrandMark from './BrandMark.astro';
import { siteConfig } from '../config/site';
---

<header class="site-header" data-site-header>
  <div class="header-shell">
    <BrandMark variant="wordmark" size="small" href="#top" ariaLabel="TSBLV — в начало страницы" />

    <nav class="desktop-nav" aria-label="Основная навигация">
      <ul>
        {siteConfig.navigation.map((item) => <li><a href={item.href}>{item.label}</a></li>)}
      </ul>
    </nav>

    <div class="header-actions">
      <button class="theme-toggle" type="button" data-theme-toggle aria-label="Включить тёмную тему" title="Включить тёмную тему">
        <svg class="theme-icon theme-icon--sun" viewBox="0 0 24 24" aria-hidden="true">
          <circle cx="12" cy="12" r="3.25"></circle>
          <path d="M12 2.5v2M12 19.5v2M21.5 12h-2M4.5 12h-2M18.72 5.28l-1.42 1.42M6.7 17.3l-1.42 1.42M18.72 18.72l-1.42-1.42M6.7 6.7 5.28 5.28"></path>
        </svg>
        <svg class="theme-icon theme-icon--moon" viewBox="0 0 24 24" aria-hidden="true">
          <path d="M20.2 15.2A8.5 8.5 0 0 1 8.8 3.8a8.5 8.5 0 1 0 11.4 11.4Z"></path>
        </svg>
      </button>
      <a class="header-cta" href="#contact">Написать</a>
      <button class="menu-button" type="button" aria-expanded="false" aria-controls="mobile-menu" data-menu-button>
        <span class="menu-icon" aria-hidden="true"><i></i><i></i></span>
        <span class="sr-only">Открыть меню</span>
      </button>
    </div>
  </div>

  <nav class="mobile-menu" id="mobile-menu" aria-label="Мобильная навигация" data-mobile-menu hidden>
    <ul>
      {siteConfig.navigation.map((item) => <li><a href={item.href}>{item.label}</a></li>)}
    </ul>
    <a class="button button-light" href="#contact">Обсудить задачу</a>
  </nav>
</header>

<style>
  .site-header {
    position: fixed;
    z-index: 50;
    top: 0.7rem;
    left: 50%;
    width: min(calc(100% - 1.5rem), 64rem);
    transform: translateX(-50%);
    transition: top 340ms var(--ease-out), width 340ms var(--ease-out);
  }

  .header-shell,
  .mobile-menu {
    border: 1px solid rgba(17, 19, 23, 0.16);
    background: linear-gradient(120deg, rgba(248, 250, 251, 0.66), rgba(220, 229, 235, 0.54));
    box-shadow: 0 0.8rem 2.4rem rgba(25, 30, 36, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.82);
    backdrop-filter: blur(24px) saturate(132%);
    transition: background 520ms var(--ease-out), border-color 520ms var(--ease-out), box-shadow 520ms var(--ease-out);
  }

  :global(html[data-theme='dark']) .header-shell,
  :global(html[data-theme='dark']) .mobile-menu {
    border-color: rgba(255, 255, 255, 0.2);
    background: linear-gradient(120deg, rgba(35, 39, 45, 0.57), rgba(18, 21, 26, 0.5));
    box-shadow: 0 0.8rem 2.4rem rgba(0, 0, 0, 0.22), inset 0 1px 0 rgba(255, 255, 255, 0.26);
  }

  .header-shell {
    position: relative;
    display: flex;
    height: 3.35rem;
    align-items: center;
    gap: 1.35rem;
    padding: 0.35rem 0.48rem 0.35rem 1rem;
    border-radius: 1.05rem;
  }

  .header-shell::after {
    position: absolute;
    top: 1px;
    right: 7%;
    left: 7%;
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent 0 9%,
      rgba(181, 31, 43, 0.44) 18%,
      rgba(255, 255, 255, 0.7) 35%,
      transparent 48% 59%,
      rgba(255, 255, 255, 0.62) 69%,
      rgba(240, 201, 40, 0.48) 79%,
      transparent 92%
    );
    content: '';
    pointer-events: none;
  }

  .site-header.is-scrolled {
    top: 0.5rem;
    width: min(calc(100% - 1.5rem), 61.5rem);
  }

  .site-header.is-scrolled .header-shell {
    background: linear-gradient(120deg, rgba(248, 250, 251, 0.72), rgba(220, 229, 235, 0.62));
  }

  :global(html[data-theme='dark']) .site-header.is-scrolled .header-shell {
    background: linear-gradient(120deg, rgba(35, 39, 45, 0.65), rgba(18, 21, 26, 0.58));
  }

  .header-shell :global(.brand-mark) {
    --brand-color: #171a1f;
    --brand-muted: rgba(23, 26, 31, 0.62);
    --brand-line: rgba(23, 26, 31, 0.24);
  }

  :global(html[data-theme='dark']) .header-shell :global(.brand-mark) {
    --brand-color: #f8fafb;
    --brand-muted: rgba(248, 250, 251, 0.68);
    --brand-line: rgba(255, 255, 255, 0.3);
  }

  .desktop-nav {
    margin-left: auto;
  }

  .desktop-nav ul {
    display: flex;
    gap: clamp(0.85rem, 2vw, 1.7rem);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .desktop-nav a {
    color: rgba(17, 19, 23, 0.7);
    font-size: 0.73rem;
    font-weight: 700;
    text-decoration: none;
    transition: color 320ms var(--ease-out);
  }

  .desktop-nav a:hover,
  .desktop-nav a:focus-visible {
    color: #111317;
  }

  :global(html[data-theme='dark']) .desktop-nav a {
    color: rgba(255, 255, 255, 0.76);
  }

  :global(html[data-theme='dark']) .desktop-nav a:hover,
  :global(html[data-theme='dark']) .desktop-nav a:focus-visible {
    color: #fff;
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 0.42rem;
  }

  .theme-toggle,
  .menu-button {
    position: relative;
    display: grid;
    width: 2.4rem;
    height: 2.4rem;
    place-items: center;
    padding: 0;
    border: 1px solid rgba(17, 19, 23, 0.14);
    border-radius: 0.75rem;
    background: rgba(255, 255, 255, 0.38);
    color: #171a1f;
    cursor: pointer;
    transition: transform 320ms var(--ease-out), background-color 320ms var(--ease-out), border-color 320ms var(--ease-out);
  }

  .theme-toggle:hover,
  .menu-button:hover {
    border-color: rgba(240, 201, 40, 0.5);
    background: rgba(255, 255, 255, 0.64);
    transform: translateY(-1px);
  }

  :global(html[data-theme='dark']) .theme-toggle,
  :global(html[data-theme='dark']) .menu-button {
    border-color: rgba(255, 255, 255, 0.18);
    background: rgba(255, 255, 255, 0.09);
    color: #fff;
  }

  :global(html[data-theme='dark']) .theme-toggle:hover,
  :global(html[data-theme='dark']) .menu-button:hover {
    background: rgba(255, 255, 255, 0.16);
  }

  .theme-icon {
    position: absolute;
    width: 1rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-width: 1.6;
    transition: opacity 320ms var(--ease-out), transform 380ms var(--ease-out);
  }

  .theme-icon--moon {
    opacity: 0;
    transform: scale(0.6) rotate(-25deg);
  }

  :global(html[data-theme='dark']) .theme-icon--sun {
    opacity: 0;
    transform: scale(0.6) rotate(25deg);
  }

  :global(html[data-theme='dark']) .theme-icon--moon {
    opacity: 1;
    transform: none;
  }

  .header-cta {
    padding: 0.66rem 0.9rem;
    border: 1px solid rgba(17, 19, 23, 0.18);
    border-radius: 0.75rem;
    background: rgba(17, 19, 23, 0.88);
    color: #f8fafb;
    font-size: 0.7rem;
    font-weight: 760;
    text-decoration: none;
    transition: transform 320ms var(--ease-out), background-color 320ms var(--ease-out), border-color 320ms var(--ease-out);
  }

  .header-cta:hover {
    border-color: var(--flag-yellow);
    background: #171a1f;
    transform: translateY(-1px);
  }

  :global(html[data-theme='dark']) .header-cta {
    border-color: rgba(255, 255, 255, 0.24);
    background: rgba(255, 255, 255, 0.12);
  }

  :global(html[data-theme='dark']) .header-cta:hover {
    background: rgba(255, 255, 255, 0.19);
  }

  .menu-button,
  .mobile-menu {
    display: none;
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
  }

  @media (max-width: 47.99rem) {
    .site-header {
      top: 0.65rem;
      width: calc(100% - 1.5rem);
    }

    .desktop-nav,
    .header-cta {
      display: none;
    }

    .header-shell {
      height: 3.2rem;
      padding-left: 0.9rem;
    }

    .menu-button {
      display: grid;
    }

    .menu-icon {
      display: grid;
      width: 1rem;
      gap: 0.25rem;
    }

    .menu-icon i {
      display: block;
      height: 1px;
      background: currentColor;
      transition: transform 320ms var(--ease-out);
    }

    .menu-button[aria-expanded='true'] i:first-child {
      transform: translateY(2.5px) rotate(45deg);
    }

    .menu-button[aria-expanded='true'] i:last-child {
      transform: translateY(-2.5px) rotate(-45deg);
    }

    .mobile-menu {
      display: grid;
      gap: 0.8rem;
      margin-top: 0.45rem;
      padding: 1rem;
      border-radius: 1rem;
      opacity: 0;
      transform: translateY(-0.5rem) scale(0.985);
      transition: opacity 320ms var(--ease-out), transform 360ms var(--ease-out);
    }

    .mobile-menu.is-open {
      opacity: 1;
      transform: none;
    }

    .mobile-menu ul {
      display: grid;
      margin: 0;
      padding: 0;
      list-style: none;
    }

    .mobile-menu li + li {
      border-top: 1px solid rgba(17, 19, 23, 0.12);
    }

    .mobile-menu a:not(.button) {
      display: flex;
      min-height: 2.7rem;
      align-items: center;
      color: rgba(17, 19, 23, 0.9);
      font-size: 0.88rem;
      font-weight: 700;
      text-decoration: none;
    }

    :global(html[data-theme='dark']) .mobile-menu li + li {
      border-top-color: rgba(255, 255, 255, 0.14);
    }

    :global(html[data-theme='dark']) .mobile-menu a:not(.button) {
      color: rgba(255, 255, 255, 0.9);
    }

    .mobile-menu[hidden] {
      display: none;
    }
  }
</style>
