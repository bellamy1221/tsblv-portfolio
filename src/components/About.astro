<section class="section about" id="about" aria-labelledby="about-title">
  <div class="site-container content-grid about-layout">
    <div class="about-heading" data-motion="clip">
      <p class="section-label">Обо мне</p>
      <h2 id="about-title">Вы работаете напрямую со мной</h2>
      <span class="availability"><i></i>Открыт к новым задачам</span>
    </div>

    <div class="about-copy" data-motion="text">
      <p>Без менеджеров и передачи задачи между разными людьми. Я сам обсуждаю проект, предлагаю решения, делаю сайт и остаюсь на связи после запуска.</p>
    </div>

    <div class="about-rail" data-sequence>
      <div class="rail-line" data-motion="line"></div>
      <div class="rail-grid">
        <div class="rail-item" data-pointer-light data-motion="row"><span>01</span><strong>Прямое общение</strong></div>
        <div class="rail-item" data-pointer-light data-motion="row" data-motion-delay="70"><span>02</span><strong>Понятные этапы</strong></div>
        <div class="rail-item" data-pointer-light data-motion="row" data-motion-delay="140"><span>03</span><strong>Один человек отвечает за результат</strong></div>
        <div class="rail-item" data-pointer-light data-motion="row" data-motion-delay="210"><span>04</span><strong>Поддержка после запуска</strong></div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    border-bottom: 1px solid var(--line);
  }

  .about-layout {
    align-items: start;
    row-gap: clamp(2.5rem, 5vw, 4.5rem);
  }

  .about-heading {
    grid-column: 1 / span 5;
  }

  .about-heading h2 {
    max-width: 42rem;
  }

  .availability {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    margin-top: 1.4rem;
    color: var(--text-muted);
    font-size: 0.7rem;
    font-weight: 650;
  }

  .availability i {
    width: 0.48rem;
    height: 0.48rem;
    border-radius: 50%;
    background: var(--yellow);
    box-shadow: 0 0 0 0 rgba(228, 185, 31, 0.28);
    animation: availability 3.5s ease-out infinite;
  }

  .about-copy {
    grid-column: 7 / -1;
    padding-top: 2.6rem;
  }

  .about-copy p {
    max-width: 52rem;
    margin: 0;
    font-size: clamp(1.55rem, 3vw, 3rem);
    font-weight: 510;
    line-height: 1.18;
    letter-spacing: -0.058em;
  }

  .about-rail {
    position: relative;
    grid-column: 1 / -1;
    padding-top: 1.25rem;
  }

  .rail-line {
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    height: 1px;
    background: linear-gradient(90deg, var(--red), rgba(17, 19, 23, 0.28) 48%, var(--yellow));
  }

  .rail-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
  }

  .rail-item {
    --pointer-x: 50%;
    --pointer-y: 50%;
    position: relative;
    display: grid;
    min-height: 6.5rem;
    align-content: center;
    gap: 0.65rem;
    padding: 1rem clamp(0.7rem, 1.4vw, 1.25rem);
    border-right: 1px solid var(--line);
    background: radial-gradient(circle 7rem at var(--pointer-x) var(--pointer-y), rgba(255, 255, 255, 0.52), transparent 72%);
    transition: color 300ms ease, padding-left 400ms var(--ease-out);
  }

  :global(html[data-theme='dark']) .rail-item {
    background: radial-gradient(circle 7rem at var(--pointer-x) var(--pointer-y), rgba(255, 255, 255, 0.06), transparent 72%);
  }

  .rail-item:first-child { border-left: 1px solid var(--line); }
  .rail-item:hover { padding-left: 1.6rem; color: var(--red); }
  .rail-item span { color: var(--text-muted); font-size: 0.58rem; font-weight: 760; }
  .rail-item strong { font-size: 0.8rem; font-weight: 680; }

  @keyframes availability {
    50% { box-shadow: 0 0 0 0.35rem rgba(228, 185, 31, 0); }
  }

  @media (max-width: 63.99rem) {
    .about-heading,
    .about-copy { grid-column: 1 / -1; }
    .about-copy { padding-top: 0; }
  }

  @media (max-width: 47.99rem) {
    .rail-grid { grid-template-columns: 1fr; }
    .rail-item,
    .rail-item:first-child { min-height: 4.5rem; border-right: 1px solid var(--line); border-bottom: 1px solid var(--line); border-left: 1px solid var(--line); }
  }
</style>
