<section class="section about" id="about" aria-labelledby="about-title">
  <div class="site-container about-layout">
    <div class="about-heading" data-reveal>
      <p class="section-label">Обо мне</p>
      <h2 id="about-title">Работаете напрямую со мной</h2>
    </div>
    <div class="about-copy" data-reveal>
      <p class="about-lead">Без менеджеров и длинных цепочек согласований. Я сам погружаюсь в задачу, предлагаю решение, разрабатываю проект и отвечаю за результат.</p>
      <ul>
        <li><span>01</span>Прямое общение</li>
        <li><span>02</span>Понятные этапы</li>
        <li><span>03</span>Решение под конкретную задачу</li>
        <li><span>04</span>Поддержка после запуска</li>
      </ul>
    </div>
  </div>
</section>

<style>
  .about { border-top: 1px solid var(--line); }
  .about-layout { display: grid; gap: 3rem; }
  .about-heading h2 { max-width: 38rem; }
  .about-lead { max-width: 49rem; margin: 0; font-size: clamp(1.35rem, 2.7vw, 2.45rem); font-weight: 500; line-height: 1.25; letter-spacing: -.045em; }
  ul { display: grid; grid-template-columns: repeat(2, 1fr); gap: 0; margin: 3rem 0 0; padding: 0; border-top: 1px solid var(--line-strong); list-style: none; }
  li { display: flex; min-height: 4.5rem; align-items: center; gap: 1rem; border-bottom: 1px solid var(--line); color: var(--muted); font-size: .86rem; }
  li:nth-child(odd) { padding-right: 1rem; }
  li:nth-child(even) { padding-left: 1rem; border-left: 1px solid var(--line); }
  li span { color: var(--accent); font-size: .6rem; font-weight: 700; }
  @media (min-width: 64rem) { .about-layout { grid-template-columns: .7fr 1.3fr; } }
  @media (max-width: 35rem) { ul { grid-template-columns: 1fr; } li:nth-child(even) { padding-left: 0; border-left: 0; } }
</style>
